# Netlify site: marketing landing page (Next.js)
# Set this site's Base directory to `apps/landing` in Netlify.

[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
